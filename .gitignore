__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
runs/
data/shards/
*.bin
*.pt
*.lpack
wandb/
analysis/out/
ops/alerts.log
ops/audit.jsonl
ops/pending_approvals/
ops/digests/
# results/results.jsonl is intentionally TRACKED: it is the scientific
# record, and the blind-extrapolation protocol relies on git timestamps.
validation/out/
uv.lock

# the book's figures are generated into docs/book/figures and copied to docs/figures;
# both are committed so GitHub Pages can serve them

# operational logs churn constantly and regenerate; not part of the record
logs/

# transient stop request, not part of the record
STOP
